---
phase: 13-resource-card-admin
plan: 02
type: execute
depends_on: ["13-01"]
files_modified: [src/pages/admin/index.astro, src/pages/admin/resources.astro]
---

<objective>
Create admin UI for resource management: list view with edit/delete, add/edit form with image upload, category assignment.

Purpose: Enable Diane to manage resources directly without code changes.
Output: Functional admin page at /admin/resources with full CRUD capabilities.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-resource-card-admin/13-01-SUMMARY.md

**UI patterns to follow:**
@src/pages/admin/newsletter.astro - Admin page layout, sidebar nav, form styling, modal pattern
@src/pages/admin/index.astro - Login flow, authentication check, admin layout structure

**API endpoints available (from 13-01):**
- GET/POST /api/resources - List and create
- GET/PUT/DELETE /api/resources/[id] - Single resource operations
- POST /api/upload-image - Image upload returning blob URL
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Resources link to admin sidebar</name>
  <files>src/pages/admin/index.astro, src/pages/admin/newsletter.astro</files>
  <action>
Add "Resources" nav link to sidebar in both admin pages, under Content section:
- Icon: grid or folder icon (similar to existing icons)
- Link to /admin/resources
- Active state when on resources page
- Position: after Homepage link, before Communication section
  </action>
  <verify>Dev server shows Resources link in admin sidebar on both pages</verify>
  <done>Resources link visible in admin sidebar, active state works</done>
</task>

<task type="auto">
  <name>Task 2: Create resources admin page with list view</name>
  <files>src/pages/admin/resources.astro</files>
  <action>
Create src/pages/admin/resources.astro following newsletter.astro pattern:
1. Auth check with redirect to /admin if not authenticated
2. Full admin layout with sidebar (copy from newsletter.astro)
3. Page header: "Manage Resources" with description
4. "Add Resource" button at top

5. Resource list section:
   - Fetch resources on page load via /api/resources
   - Display as cards or table rows grouped by category
   - Each item shows: title, category badge, URL (truncated), custom image indicator
   - Edit and Delete buttons per item
   - Empty state: "No resources yet. Add your first resource."

6. Styles: Follow existing admin conventions (section, field, btn classes)
  </action>
  <verify>npm run dev, visit /admin/resources shows page with empty list</verify>
  <done>Resources admin page loads, shows empty state, Add button visible</done>
</task>

<task type="auto">
  <name>Task 3: Add resource form with image upload</name>
  <files>src/pages/admin/resources.astro</files>
  <action>
Add modal or inline form for add/edit resource:

1. Form fields:
   - Title (required, text input)
   - Description (required, textarea)
   - URL (required, text input with https:// validation)
   - Category (required, select dropdown with 4 options)
   - Featured (checkbox)
   - Custom Image (file input + preview)

2. Image upload:
   - File input accepting image/*
   - On file select: POST to /api/upload-image
   - Show upload progress/spinner
   - On success: show preview image, store blob URL
   - "Remove Image" button to clear and use default OG
   - Preview shows current image (custom or OG proxy URL)

3. Form actions:
   - Save: POST to /api/resources (new) or PUT to /api/resources/[id] (edit)
   - Cancel: close modal/form, reset state
   - Delete confirmation when editing existing

4. State management:
   - Track currentResourceId (null = creating new)
   - Form dirty state for unsaved changes warning
   - Success/error status messages
  </action>
  <verify>Can add new resource with custom image, edit existing, delete</verify>
  <done>Full CRUD workflow works: add with image, edit, delete with confirmation</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Resources link in admin sidebar (both pages)
- [ ] /admin/resources page loads with auth check
- [ ] Resource list displays with category grouping
- [ ] Add resource form works with all fields
- [ ] Image upload shows preview and stores URL
- [ ] Edit and delete work correctly
- [ ] Form validation prevents invalid submissions
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Full CRUD workflow functional
- UI consistent with existing admin pages
</success_criteria>

<output>
After completion, create `.planning/phases/13-resource-card-admin/13-02-SUMMARY.md`
</output>
