---
phase: 13-resource-card-admin
plan: 01
type: execute
depends_on: []
files_modified: [src/lib/resources.ts, src/pages/api/resources.ts, src/pages/api/resources/[id].ts, src/pages/api/upload-image.ts, package.json]
---

<objective>
Set up backend infrastructure for resource management: Upstash Redis storage for resources, Vercel Blob for image uploads, and CRUD API endpoints.

Purpose: Enable dynamic resource management without rebuilding site, allowing custom OG images per resource.
Output: Resource storage library, image upload API, and resource CRUD endpoints.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Prior patterns to follow:**
@src/lib/drafts.ts - Upstash Redis pattern for storage
@src/pages/api/drafts.ts - API endpoint pattern with auth
@src/pages/api/drafts/[id].ts - Dynamic route pattern
@src/content/config.ts - Resource schema (title, description, url, category, featured)

**Established patterns:**
- Upstash Redis with crypto.randomUUID() for IDs
- Cookie-based admin auth check
- JSON responses with { success: boolean, data?: T, error?: string }
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vercel Blob and create resource storage library</name>
  <files>package.json, src/lib/resources.ts</files>
  <action>
1. Install @vercel/blob: `npm install @vercel/blob`
2. Create src/lib/resources.ts following drafts.ts pattern:
   - Use REDIS_URL and REDIS_TOKEN from env (already configured)
   - Resource interface: { id, title, description, url, category, featured, imageUrl?, createdAt, updatedAt }
   - Categories: 'medical-advocacy' | 'legal-resources' | 'medical-research' | 'support-communities'
   - Functions: listResources(), getResource(id), saveResource(input), updateResource(id, data), deleteResource(id)
   - Store in Redis with key pattern: `resource:{id}`
   - List via SCAN with pattern `resource:*` (same as drafts)
   - imageUrl is optional - if null, frontend falls back to OG image proxy
  </action>
  <verify>npm run build passes, TypeScript compiles without errors</verify>
  <done>Resource library exports all CRUD functions, types defined</done>
</task>

<task type="auto">
  <name>Task 2: Create resource CRUD API endpoints</name>
  <files>src/pages/api/resources.ts, src/pages/api/resources/[id].ts</files>
  <action>
1. Create src/pages/api/resources.ts (GET list, POST create):
   - GET: Return all resources from Redis, sorted by category then title
   - POST: Create new resource, validate required fields (title, description, url, category)
   - Both require admin_auth cookie

2. Create src/pages/api/resources/[id].ts (GET single, PUT update, DELETE):
   - GET: Return single resource by ID
   - PUT: Update resource fields (partial update supported)
   - DELETE: Remove resource from Redis
   - All require admin_auth cookie

Follow exact pattern from drafts.ts and drafts/[id].ts for consistency.
  </action>
  <verify>curl -X GET localhost:4321/api/resources returns empty array (after setting auth cookie)</verify>
  <done>All CRUD endpoints work: list returns [], create adds resource, get/update/delete work by ID</done>
</task>

<task type="auto">
  <name>Task 3: Create image upload API endpoint</name>
  <files>src/pages/api/upload-image.ts</files>
  <action>
Create src/pages/api/upload-image.ts:
- POST endpoint accepting multipart/form-data with 'file' field
- Require admin_auth cookie
- Use @vercel/blob put() function to upload
- Accept images only (check Content-Type starts with 'image/')
- Max size 4MB (Vercel Blob free tier limit)
- Return { success: true, url: blobUrl } on success
- Use BLOB_READ_WRITE_TOKEN env var (Vercel provides this when Blob storage is linked)

Note: Vercel Blob needs to be enabled in project settings first. Document this in SUMMARY.md.
  </action>
  <verify>npm run build passes, endpoint compiles correctly</verify>
  <done>Upload endpoint accepts image files and returns blob URL</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` succeeds without errors
- [ ] Resource lib exports: listResources, getResource, saveResource, updateResource, deleteResource
- [ ] API routes exist: /api/resources, /api/resources/[id], /api/upload-image
- [ ] All endpoints require admin authentication
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No TypeScript errors
- Backend ready for admin UI integration
</success_criteria>

<output>
After completion, create `.planning/phases/13-resource-card-admin/13-01-SUMMARY.md`
</output>
